
<table>
	<% @array_parsed['recipes'].each do |recipe| %>
	<tr>
		<td><%= link_to(image_tag(recipe["image_url"],size:"100x100"),recipe['source_url']) %></td>
		<td><%= link_to(sanitize(recipe['title']),recipe['source_url']) %></td>
		<td><%= recipe['social_rank'] %> </td>
	</tr>
	<% end %>
</table>
